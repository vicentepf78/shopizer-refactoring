# Pre-build: ./mvnw -pl sm-shop -am package -DskipTests
# Wave 2 compose image (profiles mysql + strangler + strangler-wave2).
FROM eclipse-temurin:11-jre-alpine
RUN apk add --no-cache curl
WORKDIR /app
COPY target/shopizer.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
